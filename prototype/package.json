{
  "name": "translation-engine-prototype",
  "version": "0.1.0",
  "description": "Web prototype for Translation Engine — wizard UI demonstrating the analyze → decide → preview → commit trust loop. Backed by a Cloudflare Worker that imports the existing TS library as the grammar runtime.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:app": "vite",
    "dev:worker": "wrangler dev",
    "build": "vite build",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "wrangler": "^3.50.0"
  }
}
